{
  "name": "ahs",
  "version": "0.1.0",
  "private": true,
  "description": "Agent History Standard (AHS) — offline exchange and consumption format for coding agent session history",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
